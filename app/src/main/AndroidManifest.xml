<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dianti.convertsuhu">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Register"></activity>
        <activity android:name=".Kfahrenheit" />
        <activity android:name=".Kcelcius" />
        <activity android:name=".Fkelvin" />
        <activity
            android:name=".FCelcius"
            android:label="Fahrenheit to Celcius" />
        <activity
            android:name=".fahrenheit"
            android:label="Celcius to Fahrenheit" />
        <activity
            android:name=".kelvin"
            android:label="Celcius to Kelvin" />
        <activity
            android:name=".home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>